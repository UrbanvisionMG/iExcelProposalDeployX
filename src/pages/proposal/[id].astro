---
// Dynamic proposal route: /proposal/{id}
// The {id} will be the company name with no spaces (lowercase)
const { id } = Astro.params;

// For now, display the company ID
// Your Agent will later fetch data from your data source (Airtable, Sheets, etc.)
const proposalId = id;
const companyName = id.replace(/([a-z])([A-Z])/g, '$1 $2')
  .split(/[-_]/).map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ');
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Proposal for {companyName}</title>
    <style>
      body {
        font-family: system-ui, -apple-system, sans-serif;
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
        line-height: 1.6;
      }
      h1 {
        color: #00AD9F;
        border-bottom: 3px solid #00AD9F;
        padding-bottom: 0.5rem;
      }
      .company-id {
        background: #f0f0f0;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        font-family: monospace;
      }
      .info {
        background: #e7f5ff;
        padding: 1rem;
        border-left: 4px solid #00AD9F;
        margin: 2rem 0;
      }
    </style>
  </head>
  <body>
    <h1>Proposal for {companyName}</h1>
    
    <div class="info">
      <p><strong>Status:</strong> Ready for Agent integration</p>
      <p><strong>URL Pattern:</strong> <code>/proposal/{id}</code></p>
    </div>
    
    <p>This is a dynamic proposal page. The company identifier is:</p>
    <p class="company-id">{proposalId}</p>
    
    <h2>How Your Agent Will Use This</h2>
    <ol>
      <li>Agent takes company name (e.g., "Acme Holdings Inc")</li>
      <li>Removes spaces and lowercases: "acmeholdingsinc"</li>
      <li>Stores proposal data in your data source with this ID</li>
      <li>Returns URL: <code>iexcelproposal.netlify.app/proposal/acmeholdingsinc</code></li>
    </ol>
    
    <h2>Next Steps</h2>
    <ul>
      <li>✅ Dynamic route is working</li>
      <li>⏳ Connect to data source (Airtable, Sheets, DB)</li>
      <li>⏳ Add proposal template with actual content</li>
      <li>⏳ Configure Agent to write data and trigger deploys</li>
    </ul>
  </body>
</html>
